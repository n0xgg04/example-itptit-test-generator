import { TestCase } from "./src/classes/input-test-case";
type Props = {
    template: new (...arg: any[]) => TestCase;
    amount: number;
    inputDir: string;
    outputDir: string;
    solutionPath: string;
    baseDir?: string;
    inputFilenameTemplate?: string;
    outputFilenameTemplate?: string;
    timeout?: number;
};
export default function createTestCase({ baseDir, solutionPath, inputDir, outputDir, amount, template, inputFilenameTemplate, outputFilenameTemplate, timeout }: Props): Promise<void>;
export {};
